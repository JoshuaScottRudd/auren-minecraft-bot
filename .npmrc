; Refuse to install on a Node older than package.json's "engines" asks for, and name the version needed.
; Without this npm only warns, installs anyway, and the bot fails later on a missing language feature
; with an error that never mentions Node.
engine-strict=true
